package femtodb.core.accessor.scripts;import java.io.*;import java.util.*;import femtodb.core.table.data.*;import femtodb.core.accessor.parameter.*;public class PreNumberSortComparator implements Comparator, Serializable{    int cnt = 0;    public PreNumberSortComparator() {    }    /**     * ソート処理用に並び替える     */    public int compare(Object o1, Object o2){        double num1 = 0.0;        double num2 = 0.0;        // 数値変換エラーは最後尾        try {            num1 = Double.parseDouble((String)o1);        } catch (Exception e) {            // ASC            return 1;        }        try {            num2 = Double.parseDouble((String)o2);        } catch (Exception e) {            // ASC            return -1;        }        // 比較処理        // ASC        if (num1 > num2) {            return 1;        } else if (num1 < num2) {            return -1;        }        return 0;    }    public String toString(){        return "sort cnt=" +cnt;    }}